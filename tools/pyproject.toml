[project]
name = "smartgit-translation-tools-workspace"
version = "0.1.0"
requires-python = ">=3.10"

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "packages/smartgit-i18n-scripts",
]
